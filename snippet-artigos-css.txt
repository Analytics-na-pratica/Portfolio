/* ---------- Artigos ---------- */
  .article-grid{ display:flex; flex-wrap:wrap; gap: 24px; border-top: 1px solid var(--line); padding-top: 24px; }
  .article-card{
    display:block; width: 320px; max-width:100%;
    background: var(--bg-raised); border: 1px solid var(--line-strong);
    box-shadow: 0 10px 28px rgba(0,0,0,0.38);
    transition: border-color .2s ease, box-shadow .2s ease, transform .2s ease;
  }
  .article-card:hover{
    border-color: var(--teal-bright);
    box-shadow: 0 14px 34px rgba(0,0,0,0.48);
    transform: translateY(-3px);
  }
  .article-card-banner{ position:relative; aspect-ratio: 16/9; overflow:hidden; }
  .article-card-banner img{ width:100%; height:100%; object-fit:cover; transition: transform .3s ease; display:block; }
  .article-card:hover .article-card-banner img{ transform: scale(1.05); }
  .article-card-body{ padding: 20px; }
  .article-card-meta{ font-family: var(--mono); font-size: 12px; color: var(--teal-bright); }
  .article-card-title{ font-family: var(--serif); font-size: 20px; line-height:1.28; color: var(--ink); margin-top: 10px; }
  .article-card-desc{ margin-top: 10px; font-size: 14px; color: var(--ink-muted); }
  .article-card-cta{
    margin-top: 18px; display:flex; align-items:center; gap: 6px;
    font-family: var(--mono); font-size: 13px; color: var(--teal-bright);
  }
  .article-card:hover .article-card-cta{ color: var(--blue); }
  .article-card-cta svg{ width: 13px; height: 13px; transition: transform .2s ease; }
  .article-card:hover .article-card-cta svg{ transform: translateX(3px); }

  .empty-state{
    margin-top: 32px;
    border: 1px dashed var(--line-strong);
    padding: 40px 32px;
    max-width: 56ch;
  }
  .empty-state p{ margin:0; color: var(--ink-muted); font-size: 15px; }
  .empty-state .empty-state-sub{ margin-top: 8px; color: var(--ink-faint); font-family: var(--mono); font-size: 13px; }
